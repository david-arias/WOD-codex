# ============================================================
# El Códice del Narrador — Dependencias CORE (Fase 1-4)
# Instalar con: pip3 install -r requirements-core.txt
# Las dependencias de IA (LangChain, ChromaDB, spacy) se
# añadirán en la Fase 5 via requirements.txt completo.
# ============================================================

# ── Web Framework ──────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1

# ── Base de Datos ───────────────────────────────────────────
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1                  # Requerido por SQLAlchemy async

# ── Validación y Schemas ────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1

# ── Autenticación JWT ───────────────────────────────────────
python-jose[cryptography]==3.3.0
cryptography==43.0.3

# ── Utilidades ──────────────────────────────────────────────
httpx==0.27.2
python-multipart==0.0.17
python-dotenv==1.0.1
